{
  "name": "data-action-binder",
  "version": "0.1.0",
  "description": "Data action binder. Parse all \"data-action-*\" attributes in the DOM and bind them.",
  "main": "dist/data-action-binder.js",
  "dependencies": {
    "jquery": "^3.0.0",
    "mathjs": "^3.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/DataActionBinder.js -o dist/data-action-binder.js -t [ babelify --presets [ es2015 ] ]",
    "sample": "browserify sample.js -o dist/sample.js -t [ babelify --presets [ es2015 ] ]"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/schadocalex/data-action-binder"
  },
  "author": "Alexis Schad",
  "license": "ISC"
}
