{
  "name": "data-action-binder",
  "version": "0.3.1",
  "description": "Data action binder. Parse all 'data-action*' attributes in the DOM and bind them.",
  "main": "src/DataActionBinder.js",
  "dependencies": {
    "jquery": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "mathjs": "^3.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sample": "browserify -t babelify sample.js -o dist/sample.js",
    "build": "browserify -t babelify src/DataActionBinder.js -o dist/DataActionBinder.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/schadocalex/DataActionBinder"
  },
  "author": "Alexis Schad",
  "license": "ISC"
}
